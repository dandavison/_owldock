<template>
  <div class="field has-addons" style="width: 100%">
    <p class="control" style="width: 100%">
      <b-field>
        <b-autocomplete
          type="text"
          v-model="stepName"
          field="name"
          placeholder="Add step"
          :data="filteredCandidates"
          :open-on-focus="true"
          @select="(step) => (selected = step)"
        >
          <template slot-scope="props">
            {{
              props.option.host_country
                ? props.option.host_country.unicode_flag
                : "🌍"
            }}
            {{ props.option.name }}
          </template>
        </b-autocomplete>
      </b-field>
    </p>
    <p class="control">
      <b-button @click="selected && eventBus.$emit('add:step', selected)">
        <i class="fas fa-plus"></i>
      </b-button>
    </p>
  </div>
</template>

<script lang="ts">
import {
  ProcessRuleSet,
  ProcessStep,
} from "@/pydantic-autogenerated-interfaces";
import Vue, { PropType } from "vue";

import http from "@/http";
import eventBus from "@/event-bus";

export default Vue.extend({
  props: { process: Object as PropType<ProcessRuleSet> },

  data() {
    return {
      stepName: "",
      candidates: [] as ProcessStep[],
      eventBus,
      selected: null as ProcessStep | null,
    };
  },

  async created() {
    const currentStepIds = new Set(
      this.process.step_rulesets.map((sr) => sr.process_step.id)
    );
    let candidates: ProcessStep[] =
      (await http.fetchDataOrNull(
        `/api/process-steps/${this.process.route.host_country.code}/`
      )) || [];
    candidates = candidates.filter((s) => !currentStepIds.has(s.id));
    candidates.sort((a, b) => {
      if (a.host_country && !b.host_country) {
        return 1;
      } else if (!a.host_country && b.host_country) {
        return -1;
      } else {
        return a.name > b.name ? 1 : -1;
      }
    });
    this.candidates = candidates;
  },

  computed: {
    filteredCandidates(): ProcessStep[] {
      const stepName = this.stepName.toLowerCase();
      return this.candidates.filter((s) =>
        s.name.toLowerCase().startsWith(stepName)
      );
    },
  },
});
</script>

<style scoped></style>
